// just the logic
// here order matters, so we cannot use HashMap
// s = "aabbaac" 
//output = a2b2a2c1

{
int i=0;
int count=0;
char curr = s.charAt(i);
StringBuilder sb = new StringBuilder();

while(i<s.length() && curr == s.charAt(i))
{
count++;
i++;
}
sb.append(curr).append(count);
}
S.o.p(sb.toString());

