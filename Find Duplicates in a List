//list = [1,2,2,1,3]
// output = [1,2]
// output must also be List

// 1. Add element to hashmap

// 2.
List<Integer> result = new ArrayList<>();
for(int key : map.keySet())
{
if(map.get(key))
{
result.add(key);
}
}
S.o.p(result);


// map.keySet() ==> will gives key from map
// map.get() ==> gives values from map
