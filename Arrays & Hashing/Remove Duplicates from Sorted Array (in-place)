public class Solution
{
  public int removeDup(int[] arr)
  {
    if(arr.length == 0)
      return 0;
    int i=0;
    for(int j=1;i<arr.length;j++)
      {
      if(arr[i] != arr[j])
      {
        i++;
        arr[i] = arr[j];
      }
      }
    return i+1;
  }
}
